const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { await prisma.orderItem.deleteMany({ where: { productId: 'p-1' } }); await prisma.cartItem.deleteMany({ where: { productId: 'p-1' } }); await prisma.wishlistItem.deleteMany({ where: { productId: 'p-1' } }); await prisma.product.delete({ where: { id: 'p-1' } }); console.log('Successfully deleted p-1 and all its references'); } main().catch(console.error).finally(() => prisma.$disconnect());
